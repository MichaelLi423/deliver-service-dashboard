# 项目开发规则

- 每次完成代码或文档修改后，检查实际 diff，提交本次相关改动并推送到远程仓库；不要把已完成的修改仅留在本地工作区。
- 本机无法作为最终编译验证环境。改动必须先提交并推送，再在 Windows 环境中执行编译验证；不得以 macOS 本地编译结果替代 Windows 编译结果。
- 如果 Windows 编译失败，应根据失败结果继续修复，并再次提交、推送和执行 Windows 编译，直至通过或明确报告阻塞原因。
