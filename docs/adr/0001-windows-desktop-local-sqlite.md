# ADR-0001：Windows 桌面 + Electron/React/TypeScript + 本地 SQLite

- **状态**：已接受（客户确认，2026-08-06）
- **日期**：2026-08-06
- **关联**：TBD-16 已解决；新增能力 `local-data-persistence`；需求规格第 12 章《运行与持久化边界》

## 背景

搬迁服务工作台首版为搬迁负责人个人使用的业务记录与统计工具。此前技术栈、数据库、部署与认证形态均未确定（TBD-16 的一部分）。客户确认首版边界：仅个人使用、仅 Windows、桌面应用、本地数据、不接任何远程系统，并明确了备份恢复要求。

## 决定

1. 首版为 **Windows 桌面应用**，采用 **Electron + React + TypeScript**。
2. 全部业务数据持久化在**本机 SQLite** 数据库中，不接远程数据库、云同步或外部业务系统。
3. **不提供应用内登录/账号体系**；访问保护依赖 **Windows 操作系统账户**。
4. 数据保护采用**本地备份**：每日首次使用时自动备份并仅保留最近 7 份；支持立即手动备份到所选本地目录，以及从所选本地备份恢复（恢复前确认并验证可读，失败不覆盖当前数据）。

本决定只确认架构方向，不指定 Electron 具体版本、打包器、ORM、SQLite 访问/迁移库或数据目录（属于 apply 前实现细节）。

## 原因

- 单机、单用户、单 Windows 桌面的规模下，本地 SQLite 提供事务性单文件持久化，运维与备份最简单，且不引入网络依赖。
- 应用内账号体系在"操作系统账户已提供本机访问边界"的前提下不增加价值，反而增加复杂性与攻击面。
- 本地备份是单机数据唯一的保护手段；每日自动备份 + 最近 7 份轮转在恢复窗口与磁盘占用间取得平衡，手动备份/恢复覆盖立即与重大变更场景。

## 后果

**正面**：首版范围封闭可控，无远程基础设施、无认证与权限模型、无云同步维护成本；数据可离线完整使用；备份/恢复路径明确。

**代价/风险**：

- **无多设备同步**：数据仅存在于单台 Windows 机器，换机或换环境需通过手动备份/恢复迁移，需在交付文档中说明。
- **需要 schema 迁移机制**：本地 SQLite 数据结构升级时必须迁移现有数据，迁移失败须保留可恢复状态。
- **需要备份恢复验证**：备份与恢复路径必须可测试、失败不静默，需纳入端到端验收。
- **需要 Windows 安装包**：须产出 Windows 安装与升级形态，首次安装由部署运维人员完成。
- **原型生产重写**：`prototype/relocation-workbench/selected/relocation-workbench-selected.html` 仅为设计依据，生产实现须在 Electron/React/TypeScript 中重写，不复制原型技术代码。

## 明确不做

数据库加密、应用密码、云备份/云同步（超出已确认边界，属后续变更而非本决定范围内）。

## 参考

- 需求规格 `docs/搬迁服务工作台.md` 第 12 章《运行与持久化边界》
- 能力规格 `openspec/changes/add-relocation-service-workbench/specs/local-data-persistence/spec.md`
- 设计文档 `openspec/changes/add-relocation-service-workbench/design.md` D12/D17/D18
