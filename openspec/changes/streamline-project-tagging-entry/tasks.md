## 1. 共享项目标签编辑层

- [x] 1.1 在 renderer 的 LayerState 中增加携带固定 `projectId` 与入口来源（详情或队列）的轻量项目标签编辑状态，并让两个入口渲染同一编辑容器；提交目标不得从可变的当前选中项目推导。
- [x] 1.2 实现共享“编辑项目标签”表单，复用缓存的标签目录与 `GroupedTagPicker`，从目标项目详情或队列行的 `tagIds` 初始化按组草稿，并在标题下展示客户名称及 ECC/临时编号。
- [x] 1.3 规范化目录内有效标签 ID 并按集合比较初始值与草稿值；支持恢复原选择后回到未修改状态，且在未修改、目录加载中/失败或提交中禁用/阻止保存。
- [x] 1.4 为共享 Layer 增加向后兼容的受控脏状态与提交锁定（或等价局部守卫），保持既有表单默认事件式脏状态行为不变；处理干净、脏和提交中 Esc、取消、关闭按钮及遮罩关闭的差异。

## 2. 项目详情与队列入口

- [x] 2.1 将项目详情的分类标签展示改为稳定“项目标签”区域：有标签时按现有分组样式展示并提供含目标项目名称的“编辑标签”入口；无标签时在相同位置显示“尚未添加项目标签”及“添加标签”入口。
- [x] 2.2 在高密度项目队列既有“就近录入”操作单元格中并列可见文字“记录”和“标签”动作；标签动作须带目标项目的可访问名称、阻止行点击冒泡，并以该行 DTO 固化的项目 ID 和 `tagIds` 打开共享编辑层，不得改变当前 `selectedId`、工作区或项目详情。
- [x] 2.3 调整详情标签区和队列操作组的窄窗口呈现：详情区可纵向排列，队列动作可紧凑换行或纵向排列并保留表格横向滚动；入口保持完整可见文字、焦点样式、足够操作高度和既有可访问名称，不在表格展开项目标签。

## 3. 标签编辑保存、同步与反馈

- [x] 3.1 将保存请求限制为 `update_project` 的 `{ projectId, tagIds: normalizedTagIds }`，按最新标签目录过滤去重，允许空数组清空标签；不得混入项目资料、快速记录或其他业务字段。
- [x] 3.2 在写入成功后等待现有失效范围刷新，更新当前队列；仅当目标恰好是当前选中项目时才同步当前上下文和项目详情的标签展示，再关闭编辑层并显示成功反馈；保留业务修订号的过期响应保护，且不对 `groupedTags` 作局部乐观拼接。
- [x] 3.3 区分写入失败与写入成功后的刷新失败：前者在编辑层内以 `role="alert"` 显示具体错误、保留草稿/脏状态并可重试；后者关闭编辑层、提示“标签已保存，部分视图刷新失败”并触发或提供重读，不允许诱导重复写入。
- [x] 3.4 完成弹窗可访问交互：初始焦点进入首个可用复选框（无选项时进入取消或重试）、Layer 内焦点圈定、关闭后返回实际触发入口；中部选择区内部滚动且头部、错误反馈和底部取消/保存保持可用，提交期间锁定关闭、取消、选择与重复提交。

## 4. 标签库命名

- [x] 4.1 将顶部导航“标签管理”统一改为“标签库”，将全局标签目录维护层及数据管理菜单的管理操作统一标识为“管理标签库”，保留“所有项目共用”的目录语义和既有分组/标签创建能力。
- [x] 4.2 确保项目详情与队列的项目分配入口只使用“编辑标签”或“添加标签”，不使用“管理”；标签目录为空时在轻量编辑层提供“标签库暂无内容”及“管理标签库”文字指引，但不叠加打开第二个标签库层。

## 5. 聚焦 renderer 测试

- [x] 5.1 新增或更新 focused renderer 测试，覆盖详情有/无标签的稳定区域和入口、队列行标签入口的固定目标 ID/行 DTO 标签、阻止冒泡且不改变当前选中项目/工作区/详情、两个入口共享编辑标题及分组选择，以及顶部“标签库/管理标签库”命名。
- [x] 5.2 在 focused renderer 测试中覆盖 tag-only 保存契约、清空标签、无变化不可提交、标签目录加载/空/失败状态，以及保存成功后队列的失效重读，并仅在目标为当前选中项目时重读当前上下文和详情。
- [x] 5.3 在 focused renderer 测试中覆盖写入失败保留草稿并可重试、写入成功但刷新失败不重复写入、窄窗口稳定类/可见动作、弹窗内部滚动、初始焦点与焦点圈定，以及干净/脏/提交中 Escape、取消、遮罩关闭和焦点归还。

## 6. 验证

- [x] 6.1 运行受影响 renderer 测试文件的 focused Vitest 命令，确认项目详情、队列、共享标签编辑层、保存/失败处理和标签库命名覆盖所有规格场景。
- [x] 6.2 运行 `npm run typecheck`，确认 renderer 状态、组件与测试变更通过 TypeScript 检查。
- [x] 6.3 运行适用的 OpenSpec 验证及 `npm run verify:matrix`，确认变更制品有效且规格场景证据矩阵保持通过。

## 7. 正式规格验证矩阵

- [x] 7.1 将 `scripts/build-verification-matrix.mjs` 的规格扫描路径改为正式基线 `openspec/specs/`，移除对旧 change 的硬编码，且不自动扫描 active 或 archived change delta。
- [x] 7.2 更新 `docs/verification/scenario-map.mjs` 中因测试标题演进而失效的正式基线场景证据关键词；不为尚未进入正式基线的本 change 13 个场景添加映射。
- [x] 7.3 运行 `npm run verify:matrix` 生成正式基线矩阵并确认通过，同时运行适用的严格 OpenSpec 验证。
